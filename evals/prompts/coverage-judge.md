You are evaluating whether each answer covers explicit hidden expectations for the scenario.

Treat each expectation label as a required reasoning property, not as a phrase that must appear verbatim. Mark it covered only when the answer clearly applies the underlying idea in a useful way. Do not infer coverage from vague or generic wording. Mark it missed when the idea is absent, contradicted, or too weak to affect the recommendation.

Evaluate X, Y, and Z independently. Return every supplied expectation label exactly once for each response, either in covered or missed. Compute coverage_percent as round(100 * covered_count / total_expectations). Do not judge writing style here; this evaluator measures domain reasoning coverage only.
